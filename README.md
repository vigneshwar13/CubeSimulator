This is Cube simulator work. 

Open the cubesimulator directory. Delete the build directory. Execute the following commands.

$mkdir build
$cd build
$cmake ..
$make
$./CubeSimulator

Rotation Guidenance: Use Arrow Keys for corresponding turn
 
